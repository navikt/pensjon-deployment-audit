# Database Configuration
# For local development, use DATABASE_URL
# On Nais, the app uses DB_URL (auto-injected via envVarPrefix: DB)
DATABASE_URL=postgresql://username:password@localhost:5432/nais_audit

# GitHub API Configuration
# Option 1: GitHub App (recommended - higher rate limits, better security)
GITHUB_APP_ID=your_app_id
GITHUB_APP_PRIVATE_KEY=base64_encoded_private_key_or_raw_pem
GITHUB_APP_INSTALLATION_ID=your_installation_id

# Option 2: Personal Access Token (fallback)
# GITHUB_TOKEN=your_github_personal_access_token_here

# Nais GraphQL API
# Note: Should point to the GraphQL endpoint, typically ends with /graphql or /query
NAIS_GRAPHQL_URL=http://localhost:4242/graphql

# Development authentication (only used when NODE_ENV=development and outside Nais cluster)
DEV_NAV_IDENT=A123456

# Allowed environments for /apps/add (comma-separated, leave empty for all)
ALLOWED_ENVIRONMENTS=prod-fss,prod-gcp
